local _ = require("gettext")
return {
    name = "forcerefresh",
    fullname = _("Force Full Refresh"),
    description = _(
        [[Forces full refresh on every page turn to remove all artifacts on e-ink screen. Useful for Kindles.]]),
}
